node_modules
dist
.void
.wrangler
*.local

# Vendored wasm is regenerated from the repo build by scripts/sync-vendor.mjs.
# Only package.json is committed; the binary + glue stay out of git.
vendor/wasm-tools-wasm32-wasi/*
!vendor/wasm-tools-wasm32-wasi/package.json
